.DS_Store
dist/
release/
backup/
bin/
db/
sui
web/html
main
tmp
.sync*
*.tar.gz

# local env files
.env.local
.env.*.local

# Log files
*.log*
.cache

# Windows build artifacts
*.exe
*.zip
s-ui-windows/
sui-*.exe
sui-*.zip
windows/sui-*.exe

# Editor directories and files
.idea
.vscode

# Claude Code per-user overrides and scratch worktrees. .claude/launch.json is
# shared and stays tracked, so ignore these by name rather than the directory.
.claude/settings.local.json
.claude/worktrees/
